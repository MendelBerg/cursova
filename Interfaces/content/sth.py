# from tools import *
#
# notes = [note for note in get_arr_notes('../../') if note.code != 134]
#
# with open(f"../../data/notes.txt", "w") as file:
#     for note in notes:
#         file.write(', '.join([str(e) for e in note]) + '\n')
